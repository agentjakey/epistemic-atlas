{
  "name": "epistemic-atlas-compute-scaling-checks",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "description": "Focused deterministic-evaluation code for the Epistemic Atlas compute-scaling smoke study: fenced-JSON parsing, AJV schema validation, referential integrity (v1.5), packet-driven grounding, and cost accounting. No provider client, no credentials, no run orchestration.",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "check": "node check.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
